# Pure cache: bounded memory, LRU eviction, no persistence.
# Everything stored here can be rebuilt from Postgres.
maxmemory 256mb
maxmemory-policy allkeys-lru
save ""
appendonly no

# The container network is the security boundary (see README.md).
bind 0.0.0.0
protected-mode no
